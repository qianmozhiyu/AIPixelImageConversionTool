# 优化方案：AI 像素图转化精度与准确度提升（网格检测 + 预处理）

> 生成日期：2026-08-19
> 调研范围：全量代码审查 + TestImage 基线实测 + GitHub 开源项目源码级调研（9 个相关项目）+ 网络方法检索（约 40 项方法/论文，均附来源）
> 性能约束：2K 图（2048×2048）全流程 ≤ 10s（当前实测 4.66s，达标且有余量）
> 关联文档：`docs/CORE_PIPELINE_ALGORITHMS.md`（算法现状）、`docs/GRID_PRECISION_UPGRADE.md`（上一轮调研，本轮已对照避免重复已否定方案）

---

## 1. 现状基线与代码审查结论

### 1.1 性能与检测基线（TestImage 实测，默认参数，Python 3.14 / numpy 2.5 / skimage 0.26）

| 图名 | 尺寸 | denoise | grid_detect | extract | refine | 总耗时 | px×py | 逻辑分辨率 | conf |
|---|---|---|---|---|---|---|---|---|---|
| image01.jpg | 573×577 | 0.107 | 0.099 | 0.013 | 0.061 | 0.280 | 20.59² | 27×27 | 0.620 |
| image02.png | 2048×2048 | 1.243 | 2.825 | 0.559 | 0.035 | **4.662** | 20.00² | 102×102 | 0.808 |
| slice_01.jpg | 480×853 | 0.257 | 0.667 | 0.351 | 0.029 | 1.305 | 7.45² | 64×113 | 0.635 |
| slice_06.jpg | 451×447 | 0.106 | 0.152 | 0.024 | 0.009 | 0.291 | 21.00² | 21×20 | **0.433** |
| slice_09.jpg | 486×903 | 0.237 | 0.533 | 0.387 | 0.030 | 1.186 | 7.48² | 64×119 | 0.644 |

结论：**2K 性能达标**（4.66s ≤ 10s，余量 5.3s）。性能瓶颈排序：grid_detect 2.825s（60.6%）> denoise 1.243s（26.7%）> extract 0.559s（12.0%）。

诊断运行（image02.png 关闭降噪）：grid_detect 反而变慢（2.825→3.372s），说明降噪对检测有净收益（平滑噪声降低信号复杂度）；降噪净开销仅 +0.71s。

精度侧可疑信号：slice_01/slice_09 检出**非整数周期 7.45/7.48px**（真实源图可能为 7.5px 非整数缩放，周期未精化到亚像素精度或检测有偏差）；slice_06 置信度仅 0.433，无降级处理策略。

### 1.2 问题清单（按正确性 / 鲁棒性 / 性能 / 工程质量分级）

#### 正确性与精度（C 系列）

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| C1 | `io.to_gray`、`grid_detect.compute_edge_map/has_pixel_grid/find_phase` | **全链路基于 BT.601 灰度**：等亮度异色块边界（如蓝 vs 暗红）在灰度上梯度≈0，edge_map、投影信号、BVR、find_phase 全部看不到这些边界 | 网格线断裂 → 边界带强度判据分子端被拉低（区分度 1.08 的机理之一）；文献表明灰度梯度约漏掉 10% 的彩色边缘点 |
| C2 | `grid_detect._edge_band_strength` | **1px 宽整数位置条带采样**：AA 过渡带 1-3px 且相位非整数时，1px 条带漏采边界能量，且 4 相位采样只覆盖 p 的分数相位不覆盖亚像素偏移 | 真实周期边界带强度被系统性低估 → 真实图区分度仅 1.08（合成图 1.7-2.1）的直接机理 |
| C3 | `grid_detect._vote_period`（子谐波修正） | **修正链要求 kP ∈ ACF 峰集且 k∈2..6 整数倍**：真实案例 30px 块误检 16px（30/16=1.875 非整数），修正链数学上无法捕捉；AA 平滑使 ACF 峰变宽变矮时 kP 不在峰集，修正被跳过 | 上一轮文档记录的"隐藏翻倍"未解根因 |
| C4 | `grid_detect._vote_period` L830-836 | **FFT 分数用"距主峰距离"近似**而非候选周期自身的谱支撑：主峰邻近候选得分被系统性高估/低估 | 投票偏差 |
| C5 | `grid_detect.expand_grid_edge_guided`（L2232）、`detect_squares`（L1845） | **非整数周期下 px_i（整数）与 px（浮点）混用**：BFS 扩展邻居偏移用 px_i，px=7.48 时每块累积 0.48px 漂移，64 块后达 ~31px（靠全局正则化部分拉回）；detect_squares 粗 NMS 分箱同样用整数 | 非整数周期图的网格线定位系统性偏差 |
| C6 | `grid_detect._plausibility_gate_axis` | **门控仅 p<3 触发**：3-15px 块内纹理误检只能依赖会失效的整数倍修正链（C3） | 中间尺度纹理误检防护缺口 |
| C7 | 全流水线 | **JPEG 8×8 DCT 伪影无任何处理**（TestImage 5 张中 4 张为 jpg）：8px 周期伪影落在 min_p=3~max_p=40 候选带内，污染 FFT/ACF/边界强度投票 | 8/16/24px 处系统性投票污染；slice_01/09 的 7.45-7.48px 周期接近 8px 值得怀疑 |
| C8 | `pipeline._run_denoise_global` | **降噪结果同时供检测与提取**：检测要平滑（信噪比）、提取要保真（颜色准确），目标冲突；nl_means 把 1px AA 混合色扩散为 2-3px 渐变带，加剧 C2 | 检测信号与提取颜色互相牵制 |
| C9 | `denoise.apply_clahe` | **CLAHE 逐通道独立处理**：各通道增强幅度不同导致色相漂移 | 开启后颜色失真 |
| C10 | `pipeline._run_denoise_global` 顺序、`aa_removal` | **AA 消除后置于降噪**（先 nl_means 扩散 AA 带再消除，事倍功半）；AA 消除 RGB 量化步长 32 过粗（细微色差同类，主色选择错误或漏判）；全图 8 邻域计算无边缘 mask 预筛 | 默认关闭的 AA 消除即使开启收益也打折；开启后 2K 开销大 |

#### 鲁棒性（R 系列）

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| R1 | `grid_detect.detect` 低置信拒绝 | 仅 conf<0.05 拒绝；slice_06 的 0.433 等低置信结果直接输出，无"拒绝吸附/建议人工确认"分级 | 低质量检测结果静默传递到提取 |
| R2 | `grid_detect.find_phase` | 相位目标函数 = 灰度条带均值方差：块内规律纹理与块边界周期一致时可能偏向纹理相位；只用灰度（与 C1 同源） | 相位偏移 → 块边界错位、颜色混叠 |
| R3 | `grid_detect._local_contrast_normalize` | **固定 33px 窗口**：块周期跨度 3-40px，33px 窗对 7px 块含 4-5 个周期（归一化把周期信号本身除掉）、对 40px 块不足 1 周期（边界被当局部主体） | 归一化尺度失配，弱化低对比度增强效果 |
| R4 | `scale_detect.detect_integer_scale` + `grid_detect.detect` L1346 | runs 交叉验证要求 `runs_x == runs_y` 才启用（非正方形块场景失效）；tol=12 固定灰度容差对不同对比度图不稳；整数 lag 假设与非整数周期冲突 | 交叉验证覆盖率不足 |
| R5 | `grid_detect.detect_squares` | 阈值（side_avg 0.06 / coherence 0.5 / interior 0.5）为固定值，不随图像对比度自适应；粗 NMS 整数分箱（C5） | 低对比度图种子质量差 → 兜底等距网格概率上升 |

#### 性能（F 系列）

| # | 位置 | 问题 | 影响 |
|---|---|---|---|
| F1 | `grid_detect.detect` | `has_pixel_grid` 内部与 detect 主体对同一 norm_gray **重复计算 sig_x/sig_y**（两次 O(H·W) diff+sum） | 白送数十 ms；架构冗余 |
| F2 | `extract.extract_blocks` | 双重 Python 循环逐块处理（102×102=10404 块逐块 np.median），0.56s | 可向量化到 ~0.1s（均匀网格 reshape 路径） |
| F3 | `grid_detect.detect_squares` 贪心 NMS | Python 逐候选循环 + 3×3 邻域字典 | 2K 图数千候选下非热点但可优化 |
| F4 | 全链路 float64 | 3 个积分图各 33.7MB + 多份全图副本 | 峰值内存数百 MB；float32 可减半（注意精度验证） |
| F5 | `aa_removal.remove_anti_aliasing` | 每像素 8 邻域全量计算 + ~20 个 (H,W,3) 临时数组 × passes | 启用时 2K 开销大；可先用边缘 mask 预筛 |

#### 工程质量（Q 系列）

| # | 位置 | 问题 |
|---|---|---|
| Q1 | `core/downsample.py` | 死代码：文档已声明移除 downscale 功能，文件仍存在且无引用 |
| Q2 | `cli.py` L86 | `--upscale` help 文案"双线性放大"与实际默认 nearest 不符 |
| Q3 | `io.save_image` | 使用 Pillow 已弃用的 `Image.NEAREST` 常量（应为 `Image.Resampling.NEAREST`） |
| Q4 | `grid_detect.py` | detect() 主流程 250+ 行、新旧双路径（有/无 edge_map）并存，维护成本高 |

---

## 2. 外部调研结论

### 2.1 GitHub 开源项目（源码级调研，按相关性排序）

| 项目 | 语言/规模 | 网格检测核心策略 | 预处理策略 | 对本项目价值 |
|---|---|---|---|---|
| **lovelaced/pixfix** | Rust / 10★ | OKLAB 色差能量剖面 + 连续 (pitch, phase) 穷举梳搜索，**能量集中度打分** | 网格归一化 snap、OKLAB 三角不等式 AA 消除、量化 | ★★★★★ 原理性解决子谐波/非整数周期/等亮度边界 |
| **univeous/Pixel-Extractor** | TS+Python / 28★ | CIE76 二值边剖面 + find_peaks + **L-BFGS-B 间距格点拟合**（RMSE+缺口惩罚）+ 浮点插线采样 | 小波降噪（BayesShrink YCbCr）、Lab 量化先行 | ★★★★★ Python 栈直接移植，非整数周期全链路 |
| **dmno-dev/crispx** | Rust / 3★ | Sobel 局部对比度归一化（除窗口均值）+ coherence 连贯度判边 + ACF 谐波解释打分 + BFS 边缘引导扩展 | 逐格均值 + union-find 空间合并 | ★★★★ coherence 判据对症区分度问题（本项目已部分同源） |
| **Hugo-Dz/spritefusion-pixel-snapper** | Rust+WASM / 2991★ | **先 k=16 量化后检测** + 峰间距中位数 + 弹性游走（±0.35×step 每步重锚定）+ 跨轴稳定化 | RGB k-means 量化 | ★★★★ 预量化范式 + 游走锚定 |
| **theamusing/perfectPixel** | Python / 1920★ | FFT2 幅度谱对称峰对 + 梯度回退 + refine_grids 中心双向传播 | — | ★★★ 对称峰确认、块内 K=2 多数簇提取 |
| **KennethJAllen/proper-pixel-art** | Python / 509★ | Canny+Hough 直线族 + 截尾中位数 + homogenize 浮点细分 | 2× 预上采样助检 | ★★★ Hough 对纹理丰富 AI 图伪线多，选择性借鉴 |
| **jenissimo/unfake.js** / painebenjamin/unfake.py | JS/Rust + Python 移植 / 900★ | runs GCD + **RGBA 四通道差分 ACF**（acpeak）+ 分块 Sobel 众数 + auto 级联 | Wu 量化、形态学 2×2 开闭清理 | ★★★★ 四通道差分是等亮度边界的最低成本方案 |
| **elonen/spritesheet-cleanup** | Python / 1★ | **逐通道 Sobel** + Otsu + 间距直方图高斯拟合（浮点格尺寸）+ 空档扩窗游走 | — | ★★★ 三件套干净可抄 |
| jerith/depixel 等 Kopf 2011 实现 | Python / 223★+ | 无检测（假设输入已是标准像素图） | — | ★ 不适用（逆问题），仅输出端相似度图概念可参考 |

> 检索说明：`pixel art upscale detection`、`pixelization detection`、`remove pixelation` 等关键词无额外有效命中；Kopf《Depixelizing Pixel Art》27 个实现均无网格检测。

**跨项目共性结论**（多项目独立收敛的技术 = 高置信度借鉴对象）：
1. 检测前**颜色量化**摧毁块内纹理/渐变（spritefusion、Pixel-Extractor、unfake qvote）；
2. 感知空间（OKLab/CIE76）相邻像素色差替代灰度差分（pixfix、Pixel-Extractor、unfake）；
3. **峰间距中位数/众数**是最多采用的鲁棒周期估计量，GCD 链公认脆弱；
4. 全局周期给初值 + **局部逐线锁定（游走/BFS）**吸收漂移（spritefusion、perfectPixel、crispx、本项目已有）；
5. 块提取以**众数/优势色**为主流，均值/中位数为次选。

### 2.2 网络方法检索（按主题，均验证可访问）

**周期估计与倍频错误**：YIN/CMNDF 首凹机制（抗八度错误，输出非整数周期，<10ms）；倒谱 harmComp 峰栅格回归（EUSIPCO 2021，识别"近似倍数"误检）；QIFFT 抛物线谱峰插值（亚 bin 精度，5 行代码）。

**网格线检测**：Radon 旋转受限投影 + 投影 FFT（沿线积分弱边界 SNR 提升 √N，亚像素行距）；长线状结构元素形态学开运算（方向相干滤波，抑制 8px JPEG 不相干伪影）；STV 尺度分解思想（先分离纹理层与结构层再测峰，简化版 <1s）。

**棋盘格/标定板**（处理"非完美棋盘"的成熟经验）：ROCHADE 鞍点表面拟合；BoofCV 模糊感知金字塔（**逐角点估计局部模糊尺度再验证**——固定尺度判据区分度不足的根因之一）；模式生长验证（从最可信局部向外生长，长不出去就停）。

**2D 周期性**：2D ACF 峰"支配域"晶格恢复（PAMI 2004，px/py 联合估计——16px 假周期无法同时解释两轴峰列）；多峰 ACF 平均 + std 置信度（Nečas 2022 方法论对比：多峰平均显著优于单峰）；spritesheet_detector 的**尺寸因子枚举 + VoV + 图像域切割线验证**（候选空间来自物理因子而非频谱峰，与本项目失败案例直接对症）。

**彩色边缘**：Di Zenzo 结构张量（通道方向相干叠加：真边界增强、随机纹理抵消）；对抗通道 O1/O2/O3；cue 组合应做 divisive normalization 而非线性加权（JoV 2008）。

**JPEG 网格**：IPOL 2020 Local JPEG Grid Detector（交叉差分 + 64 相位投票 + NFA 虚警控制，免调参，<0.5s）；边界能量比 / FFT 谐波比（<0.1s）；JPEG 网格**相位固定**与像素网格相位任意是本质判别特征。

**轻量学习**：PiDiNet（<1M 参数，CPU 512² 亚秒级）优于已否定的 U-Net 路线，但需在合成退化数据上微调，列为中期备选。

### 2.3 与上一轮调研（GRID_PRECISION_UPGRADE.md）的对照

| 上一轮结论 | 本轮新认知 |
|---|---|
| Spectral Comb 主导投票：**已否定**（真实图边界区分度 1.08 使门控失效，comb 把 px 从 16 拉到 4） | 失败根因 = 频域判据缺少图像域验证 + 真实图证据弱。新解法：① pixfix 能量集中度打分（子谐波双倍覆盖必然低分，**不依赖强度比经验阈值**）；② 因子枚举 + 图像域切割线验证（频域梳降级为候选生成器，裁决权交图像域）。**不复活 comb 投票** |
| GPA 相位场：函数保留不接入 | 无新增证据，维持现状 |
| cornerSubPix：已否定（理想二值图收敛半像素） | 点对称角点检测（BMVC 2018，依赖对称性而非灰度梯度极值）为远期备选，不进第一梯队 |
| 仿射外推：已否定 | 无新增 |
| U-Net：远期（需标注+GPU） | PiDiNet 替代为更现实的轻量学习选项（训练数据可程序化合成：真实像素图 + 扩散伪影 + JPEG 退化），仍列中期 |
| 已知未解："真实图放大路径隐藏翻倍（30px→16px）"与"边界强度区分度 1.08" | 本轮定位根因（C1/C2/C3/C7）并给出对症方案（G1-G5、P1-P2），见下文 |

---

## 3. 网格检测改进方案

> 每条建议包含：改什么 / 怎么改 / 为何有效 / 性能预算 / 风险与回滚。

### G1. 检测信号升级：感知空间色差替代灰度梯度 【优先级：高】

- **改什么**：`grid_detect.compute_edge_map`、`has_pixel_grid` 的投影信号（sig_x/sig_y）、`_vote_period` 的 profile 输入；`pipeline._run_grid_detect` 的 to_gray 调用。
- **怎么改**：计算相邻像素 OKLAB 色差 `d(x,y) = ||oklab(x,y) - oklab(x±1,y±1)||`（或 Di Zenzo 结构张量梯度幅值 `λ+ = (E+G+√((E−G)²+4F²))/2`，E/F/G 为各通道 Sobel 分量的结构张量元素）。剖面信号 = 色差沿轴求和；edge_map = 色差图（或彩色梯度图）做现有局部对比度归一化。`find_phase` 的条带均值方差目标同步换用色差剖面。OKLAB 转换复用 `color.rgb_to_oklab`（向量化已存在）。
- **为何有效**：直击 C1（等亮度异色边界在灰度中梯度≈0，OKLAB 色差不为零）与 C2 部分（色差对 AA 中间色的响应中心在边界真实位置）。pixfix/Pixel-Extractor/unfake 三项目独立验证此路线；Di Zenzo 相干叠加还附带抑制随机方向纹理。预期区分度 1.08 → 1.4+（等亮度边界占比决定上限）。
- **性能预算**：OKLAB 转换 + 色差 ~0.15s（2048²）；Di Zenzo ~0.2s。
- **风险与回滚**：低。新增 `detect_signal: "gray"|"oklab"|"dizenzo"` 参数，默认 gray 保持零回归，实验切换。

### G2. 周期裁决升级：梳状能量集中度打分（pixfix 范式）【优先级：高，核心】

- **改什么**：`_vote_period` 的最终裁决（或作为新的第五判据 + 独立裁决级），替代以 `_edge_band_strength` 比值为主判据的子谐波修正链。
- **怎么改**：对候选 (pitch, phase) 连续穷举：pitch ∈ [min_p, max_p] 浮点（粗扫步长按 `pitch²/(2·L)` 自适应保证全轴漂移 ≤0.5px，再步长折半局部精化）；相位粗扫 0.5px / 细扫 0.1px。打分：
  `score(pitch, phase) = Σ梳线捕获能量 / 总能量 − Σ梳线覆盖权重 / 总权重`
  其中能量剖面 = G1 的色差剖面。规则移植：平局集（最高分 ×1.15 内）取最大 pitch；整数 pitch 得分 ≥0.97× 浮点最优才吸附整数；置信度 = quality × separation（separation = 相对最优非谐波候选的领先幅度），低于阈值（pixfix 用 0.35）拒绝吸附、输出 best guess 并降级 conf。
- **为何有效**：**子谐波 pitch/2 以双倍覆盖捕获同样能量 → 得分数学上必然更低；谐波 2×pitch 漏掉一半真实边 → 得分必然更低**。不依赖"边界强度比 >1.3"这类在真实图上坍缩的经验阈值（区分度 1.08 的直接解法），且连续 pitch 穷举天然覆盖非整数周期与非整数倍误检（C2/C3/C6 三连解）。pixfix 带 15 个单元测试覆盖分数周期/谐波纹波/AA 噪声/跨轴和解场景。
- **性能预算**：向量化（`np.add.reduceat` / 花式索引按梳线分组求和）后，每候选 O(L/pitch)；全候选粗扫+精化估计 0.5-1s（2048² 剖面长 2048）。可通过下采样剖面（1024）再减半。
- **风险与回滚**：中。打分函数新增 + 投票重构，需以 TestImage + 合成回归集验证；`enable_comb_energy_score` 开关默认关，验证后切换。
- **实施提示**：保留现有 ACF/FFT/BVR 判据做候选生成与交叉验证，G2 作为终审裁决级——与 spritesheet_detector "频域降级为候选生成器"的结论一致。

### G3. 周期精化：峰值格点拟合（Pixel-Extractor 范式）【优先级：高】

- **改什么**：`detect()` 中 px/py 确定后的精化级（find_phase 之前），及 `Grid.px/py` 输出精度。
- **怎么改**：对 G1 剖面 `scipy.signal.find_peaks(profile, prominence=...)` 取峰 → 按 prominence 降序取前 k 峰（k 从小扫描到大）→ 对每个 k 用 `scipy.optimize.minimize(L-BFGS-B)` 拟合 spacing：目标 = `RMSE(实际间距 − round(间距/s)·s)/s + 缺失格比例惩罚`，取误差最小的 (k, s)。虚峰通过减小 k 自然排除。输出的浮点 spacing 直接作为 px/py。
- **为何有效**：直接输出非整数周期（slice_01 的 7.45 / slice_09 的 7.48 应收敛到稳定浮点值）；30px 真周期的峰列在 30/60/90 处，16px 误检不在该栅格上——格点拟合天然排斥（C3 的另一道防线）。原实现即 Python/scipy，近乎直接移植（`worker.js` 内 `find_optimal_spacing/get_spacing_error` 四函数）。
- **性能预算**：<0.1s（2048 点剖面 + L-BFGS-B）。
- **风险与回滚**：低。`enable_peak_lattice_fit` 开关；拟合失败回退现有周期。

### G4. 候选空间扩展：尺寸因子枚举 + 图像域验证【优先级：中】

- **改什么**：`_vote_period` 的候选收集（现在仅 FFT 主峰 + ACF 峰 + 谐波基频 + comb top5）。
- **怎么改**：候选并集加入**图像尺寸因子**：`W/k ± 容差`（k 遍历使候选落在 [min_p, max_p]），再做近似谐波归约（ spritesheet_detector 方式）。每个候选在 512² 下采样图上做 VoV（方差之方差：真网格块内均匀块间不同 → 高分）与切割线内容验证（切割线是否穿过块内部纹理/空白）。
- **为何有效**：候选不再只来自频谱峰（频谱峰被纹理污染时真实周期根本不进候选集，修正链无从谈起）；图像域硬验证对非整数倍误检（C3）结构性免疫。
- **性能预算**：0.3-0.5s（下采样验证）。
- **风险与回滚**：低。仅扩大候选集，裁决逻辑不变。

### G5. JPEG 网格检测与显式降权【优先级：中高（测试图 4/5 为 jpg）】

- **改什么**：`detect()` 投票前的预处理判据。
- **怎么改**：交叉差分 `C(x,y) = I(x+1,y+1) − I(x+1,y) − I(x,y+1) + I(x,y)`（×形二阶差分，真实内容边缘被抵消、量化阶跃残留强响应）→ 滑窗局部极大对 64 个可能网格原点投票 → 峰显著性检验（IPOL NFA 简化版）。检测到显著 8px 网格时：① 投票中对 8/16/24px 候选乘惩罚系数（JPEG 网格与块尺寸无关，是加性干扰源）；② 交叉差分图本身作为低权重互补判据（对量化阶跃敏感、对内容边缘不敏感）。
- **为何有效**：直击 C7。JPEG 网格周期固定 8px、**相位固定**（网格原点），与像素网格相位任意是本质差异；色度下采样使色度伪影形态与亮度不同（联动 G1 的色差信号可做 p≈8 歧义仲裁）。
- **性能预算**：交叉差分 <0.1s + 相位投票 ~0.3s。
- **风险与回滚**：低。`jpeg_grid_guard` 开关；仅降权不删除候选（真实 8px 像素网格仍可通过其他判据胜出——其相位与 JPEG 相位通常不对齐）。

### G6. 2D 晶格联合约束 + 自适应置信度【优先级：中】

- **改什么**：`detect()` 的 px/py 独立决策（现在 x/y 轴分别投票后仅在 px≈py 时加权平均）。
- **怎么改**：2D ACF（rfft2，复用现有 FFT 基建）峰检测用"支配域"法取两个最大非共线位移矢量做晶格基向量候选；px/py 候选对必须能共同解释 2D ACF 峰列（16px 假周期无法同时解释两轴峰列 → 否决）。多峰 ACF 间距的均值 ± std 输出逐图置信度，用于动态调整多判据投票权重（固定权重在弱信号图上失效——slice_06 类）。
- **为何有效**：单轴判据不具备的第二维否决机制（PAMI 2004 已验证）；std 置信度免费获得。
- **性能预算**：rfft2 2048² ~0.3-0.6s（可下采样 1024² 减半）。
- **风险与回滚**：中。仅做否决/加权，不改变主检测路径。

### G7. 检测链路一致性修复【优先级：高（低成本）】

- **改什么/怎么改**：
  1. `detect()` 消除 sig_x/sig_y 重复计算（has_pixel_grid 返回投影信号或外部统一计算一次）——F1；
  2. `_vote_period` 的 FFT 分数改为候选周期处的谱幅值（谐波频率线性插值），删除"距主峰距离"近似——C4；
  3. `expand_grid_edge_guided` / `detect_squares` 的整数 px_i 偏移与分箱改为浮点周期取整仅在像素采样点，扩展偏移用浮点 px（`_fit_global_regularization` 已是浮点，统一之）——C5；
  4. `_plausibility_gate_axis` 触发条件从 p<3 放宽为 p<min_p×2 或改由 G2 打分接管（G2 落地后此门控可弱化为兜底）——C6；
  5. 低置信分级：conf < 0.4 时在 Grid 元信息标记 `low_confidence` 供 GUI 提示人工确认（R1）。
- **为何有效/性能**：均为小改动，合计 <0.1s 净变化（F1 还略微提速）。
- **风险与回滚**：低。逐项独立开关/逐项提交。

---

## 4. 预处理改进方案

> **总原则（P1 先行）**：所有"检测信号增强"类预处理只作用于**检测分支的副本图像**，块提取仍走原降噪图——颜色保真度副作用归零，现有"去噪-检测耦合保护"思路的推广。

### P1. 检测/提取双分支分离【优先级：最高（架构前提）】

- **改什么**：`pipeline._run_denoise_global` / `_run_grid_detect`。
- **怎么改**：denoise_global 输出两份：`denoise_global`（提取路径，保守：现有 nl_means 或更轻）与 `detect_signal_source`（检测路径：nl_means → P2 量化 → 可选 P4/P5 增强）。grid_detect 消费后者，extract 消费前者。新增参数 `split_detect_branch: bool = True`。
- **为何有效**：解除 C8 目标冲突——检测可以激进增强（量化/冲击滤波都会破坏颜色保真，但现在无所谓），提取保持颜色准确。这也是下述所有预处理建议的启用前提。
- **性能预算**：检测分支的量化等操作在下采样或全图 ~0.3-0.5s；提取路径可考虑降噪减档（实验决定）。
- **风险与回滚**：低。开关默认关时行为与现状完全一致。

### P2. OKLab 预量化：升级现有开关并默认启用【优先级：最高（对症区分度 1.08）】

- **改什么**：`pipeline._quantize_detection_signal`（现有 16 色中位切分、默认关）。
- **怎么改**：① 空间从 RGB 升级为 OKLAB（`color.rgb_to_oklab` 已有）；② K 自适应：从 K=8 起倍增，准则为"箱内最大色散 < 块间最小色距的一半"（或量化前后 ΔE 分布分位数），上限 32；③ 下采样 1/16 像素建箱（median-cut）+ 全图 LUT 映射加速；④ 默认启用（经 P1 后只影响检测分支）。
- **为何有效**：量化是唯一**同时双向作用**的预处理——分子端：块边界阶跃化（AA 1-3px 过渡带 → 1px 阶跃，Sobel/色差能量集中 2-3 倍）；分母端：块内渐变/纹理坍缩为零梯度箱。本质是把真实图的统计分布推向合成图（区分度 1.7-2.1 的来源分布）。spritefusion（k=16 量化后检测，2991★）、Pixel-Extractor（Lab 量化先行）、unfake（Wu 量化）三项目共同采用；unfake 文中明确将量化列为"消除柔和渐变、抗锯齿、随机色斑"手段。附带压制 JPEG 色噪声（C7 部分）。
- **性能预算**：~0.3s（下采样建箱 + LUT）。
- **风险与回滚**：低（P1 前提下）。风险点：弱边界两侧色差小于量化箱距时边界被误合并——自适应 K 准则就是为此设计；`enable_pre_quantize` 开关保留。

### P3. AA 消除的位置与参数修正【优先级：中】

- **改什么**：`pipeline._run_denoise_global` 内 AA removal 的调用位置与 `aa_removal` 参数。
- **怎么改**：① AA 消除移到降噪**之前**（或检测分支内降噪减档/关闭）；② RGB 量化步长 32 → 16（或按图像色距分布自适应）；③ 仅对边缘 mask 候选像素计算（先色差阈值预筛，预期 90%+ 像素跳过）。
- **为何有效**：C10——先 nl_means 再消 AA 是顺序倒置（平滑滤波先扩散 AA 带）；步长 32 过粗导致主色错分；全图计算是性能浪费。
- **性能预算**：修正后启用 AA 消除 ~0.3-0.6s（mask 预筛后）。
- **风险与回滚**：低。AA 消除本身保持默认关闭或仅检测分支启用。

### P4. 冲击滤波（检测分支可选增强）【优先级：中低】

- **改什么**：检测分支在 P2 量化之后的可选级。
- **怎么改**：Osher-Rudin 冲击滤波 `I^t = -sign(ΔI)·|∇I|`（先 σ≈0.8 高斯预平滑抗噪，5 迭代显式格式，纯 numpy ~50 行；或 Kass-Solomon smoothed shock filter 变体）。
- **为何有效**：对"AA 过渡带变宽"的物理逆操作——Laplacian 拐点区制造阶跃断裂，把分散的 AA 能量推回边界真实位置；TV 守恒不引入振铃；块内弱纹理被段内平滑。量化（P2）已达成大部分效果，此项为**补充手段**（P2 实测区分度仍不足时启用）。
- **性能预算**：~0.5s。
- **风险与回滚**：低（检测分支专用）。作用过度会产生过冲，迭代数保守起步。

### P5. 多尺度局部对比度归一化 + 亮度通道 CLAHE【优先级：中】

- **改什么**：`_local_contrast_normalize`（固定 33px 窗）、`apply_clahe`（逐通道）。
- **怎么改**：① LCN 改 2-3 尺度并行（如 12/24/48px 窗）+ 逐尺度 std 钳位 + 逐像素取最大响应；② CLAHE 转 Lab 只对 L 通道做（消除色相漂移 C9）。
- **为何有效**：R3——块周期 3-40px 跨 13 倍，33px 单窗对 7px 块把周期信号本身归一化掉、对 40px 块不足一个周期；多尺度覆盖全跨度。BoofCV 模糊感知金字塔的结论（逐点估计局部尺度再验证）与此同源。
- **性能预算**：多尺度 LCN ~0.2-0.4s；Lab CLAHE 与现状持平。
- **风险与回滚**：低。

### P6. 轻量中值 AA 阶跃化（备选）【优先级：低】

- 3×3 加权中值（中心权重加大）1-2 遍：中值滤波根信号性质保证阶跃边缘不变、AA 过渡带坍缩到单侧块色。cv2.medianBlur 30-50ms / skimage ~0.3-1s。与 P2 叠加边际递减，作为无量化路径的低开销备选。风险：细于窗口的 1px 纹理线被移除。

---

## 5. 落地优先级与性能预算

### 5.1 分梯队路线图

| 梯队 | 内容 | 针对问题 | 依赖 |
|---|---|---|---|
| **一（先做）** | P1 双分支分离 → P2 OKLab 预量化 → G1 感知空间检测信号 → G3 峰值格点拟合 → G7 一致性修复 | C1/C2/C8/C3(非整数周期)/C4/C5/F1 | 无，可并行开发 |
| **二（核心升级）** | G2 梳状能量集中度打分 → G5 JPEG 网格降权 | C2/C3/C6/C7、区分度 1.08 根治 | 梯队一的 G1 色差剖面 |
| **三（增强与工程化）** | G4 因子枚举验证 → G6 2D 晶格约束 → P4 冲击滤波 → P5 多尺度 LCN → P3 AA 修正 → E1 提取向量化 → E2 工程清理（Q1-Q4） | 覆盖率与鲁棒性增强、性能 | 视梯队一二实测效果取舍 |

### 5.2 性能预算核算（image02.png 2048×2048）

| 方案组合 | denoise | 检测分支预处理 | grid_detect | extract | refine | 总计 |
|---|---|---|---|---|---|---|
| 现状基线 | 1.24 | — | 2.83 | 0.56 | 0.04 | 4.66 |
| 梯队一 | 1.24 | P2 0.3 + G1 0.15 | 2.83（G3/G7 ≈±0） | 0.56 | 0.04 | **≈5.1** |
| 梯队一+二 | 1.24 | 0.45 | 2.83 + G2 0.5~1 + G5 0.4 | 0.56 | 0.04 | **≈6.0~6.5** |
| 全量（视需要） | — | +P4/P5 0.5~0.9 | +G4/G6 0.6~1 | E1 后 ~0.1 | 0.04 | **≈7~7.5** |

结论：全部建议叠加仍在 10s 预算内并留 25% 余量；且 G2 落地后可**替换**现有部分判据（子谐波修正链的多次 `_edge_band_strength` 调用），检测阶段有望持平甚至下降。

### 5.3 验证与回归方案（每梯队交付前执行）

1. **合成精度回归集**（上一轮文档已指出缺失）：真实干净像素图（程序生成或精选）+ 受控退化（JPEG 质量 60-90、AA 混合 1-3px、高斯噪声、非整数缩放 7.5×）+ 已知逻辑分辨率 → 断言 px/py/w_logic/h_logic 误差。重点用例：非整数倍纹理误检（30px 块 + 16px 纹理）、非整数周期（7.5×）、等亮度异色块、JPEG 压缩图。
2. **TestImage A/B**：5 张图的检测结果与耗时对比表（本文 1.1 为基线）。
3. **区分度指标直接测量**：对每图计算 `D = edge_band(true_p) / edge_band(best_subharmonic)`，验证 1.08 → 1.5+ 的改进幅度。
4. **现有测试零回归**：pytest 全量通过；所有新路径以默认关闭参数合入，逐项开启验证后切换默认值。

### 5.4 风险与回滚策略

- 所有建议均带独立开关（`PipelineParams` 新参数默认关闭或经实验验证后启用），可逐项回滚；
- 每项按"实现 → 合成回归 → TestImage A/B → 切默认"四步推进，任一步不达标即停留在开关后；
- G2 为最大改动（投票重构）：建议以"新增裁决级 + 保留旧路径"方式合入，跑通对比后再删除旧子谐波修正链；
- 对外行为不变：`Grid` API、`PipelineResult` 结构、CLI/GUI 参数兼容。

---

## 附录：主要参考来源

**GitHub 项目**：lovelaced/pixfix（https://github.com/lovelaced/pixfix）· univeous/Pixel-Extractor（https://github.com/univeous/Pixel-Extractor）· dmno-dev/crispx（https://github.com/dmno-dev/crispx）· Hugo-Dz/spritefusion-pixel-snapper（https://github.com/Hugo-Dz/spritefusion-pixel-snapper）· theamusing/perfectPixel（https://github.com/theamusing/perfectPixel）· KennethJAllen/proper-pixel-art（https://github.com/KennethJAllen/proper-pixel-art）· jenissimo/unfake.js（https://github.com/jenissimo/unfake.js）· painebenjamin/unfake.py（https://github.com/painebenjamin/unfake.py）· elonen/spritesheet-cleanup（https://github.com/elonen/spritesheet-cleanup）· mirsella/spritesheet_detector（https://github.com/mirsella/spritesheet_detector）

**方法/论文**：YIN（de Cheveigné & Kawahara 2002，https://metricgate.com/docs/yin-pitch-detector/）· 倒谱 harmComp（EUSIPCO 2021，https://eurasip.org/Proceedings/Eusipco/Eusipco2021/pdfs/0000371.pdf）· QIFFT（https://www.dsprelated.com/freebooks/sasp/Sinusoidal_Peak_Interpolation.html）· Radon 行距（https://gist.github.com/endolith/334196bac1cac45a4893）· ROCHADE（ECCV 2014，https://www5.cs.fau.de/fileadmin/research/Publikationen/2014/Placht14-RRC.pdf）· BoofCV 模糊感知角点（arXiv 2110.13793）· 晶格恢复（Liu/Collins PAMI 2004，https://www.cse.psu.edu/~rtc12/Papers/liu04computational.pdf）· 光栅周期测量对比（Nečas 2022，https://arxiv.org/pdf/2208.12151v1.pdf）· Di Zenzo 彩色梯度（1986；改进 Jin 2012）· 对抗通道边缘（CVPR 2013）· cue 组合（Zhou & Mel JoV 2008）· IPOL JPEG 网格检测（https://www.ipol.im/pub/art/2020/283/article.pdf）· Kim-Park JPEG 去块（信号自适应后处理）· 冲击滤波（Osher-Rudin 1990，https://www.cs.jhu.edu/~misha/ReadingSeminar/Papers/Osher90.pdf；smoothed 变体 ICPR 2012）· 引导滤波（He et al. TPAMI 2013）· Kuwahara 变体 · Kopf & Lischinski 2011（Depixelizing Pixel Art）· PiDiNet（ICCV 2021，https://github.com/hellozhuo/pidinet）· unfake.js 作者实践文（https://dev.to/jenissimo/how-to-tame-your-ai-pixel-art-3pk5）· 中值滤波根信号理论 · MMCQ 量化（https://modern-colorthief.readthedocs.io/en/latest/mmcq.html）

**结论摘要**：本轮调研的核心发现是——上一轮在**检测判据侧**（comb/GPA/cornerSubPix）的失败，其根因在于真实图的检测信号本身太弱（灰度化丢边界 + AA 使能量分散 + JPEG 伪影污染 + 预处理增益未利用）。对症药方在**预处理侧**（P1/P2：分支分离 + OKLab 预量化）与**裁决范式侧**（G2：能量集中度打分替代经验强度比；G3/G4：连续周期 + 图像域验证替代整数倍修正链），且均有开源项目源码级验证与单元测试背书，全部可在现有 numpy/scipy 栈内落地，总性能预算 ≈7s，满足 2K ≤10s 约束。
